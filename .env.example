# Database (local development)
DATABASE_URL="file:./dev.db"

# Public site origin — used for OpenGraph, canonical and sitemap URLs
NEXT_PUBLIC_SITE_URL=

# Telegram notifications (ЭТАП 4) — server-side only, never NEXT_PUBLIC_*
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# CRM auth (ЭТАП 5) — server-side only
CRM_PASSWORD=
CRM_SESSION_SECRET=

# Optional analytics — not connected yet
NEXT_PUBLIC_METRIKA_ID=
